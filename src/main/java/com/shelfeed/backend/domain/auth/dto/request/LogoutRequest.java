package com.shelfeed.backend.domain.auth.dto.request;

// body 없음 — refreshToken은 httpOnly Cookie에서 읽고, accessToken은 Authorization 헤더에서 읽음
public class LogoutRequest {
}
